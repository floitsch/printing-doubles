{
  "name": "printing-doubles",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 4173 --directory site",
    "test": "node --test",
    "check": "node scripts/check-site.mjs"
  }
}
